<Window x:Class="WPF_Windows_Spotlight.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Window Spotling" Height="350" Width="600">
    <Grid Margin="0,0,0,0">
        <TextBox x:Name="Input" Margin="0,0,0,273" FontSize="24" FontFamily="Arial" TextChanged="Search"/>
        <ListBox x:Name="QueryList" ItemsSource="{Binding}" HorizontalAlignment="Left" Height="273" Margin="0,46,0,0" VerticalAlignment="Top" Width="300">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <!--<Image Width="75" Height="75" Source="{Binding AvatarUrl}" />-->
                        <StackPanel Orientation="Vertical" Margin="5,0">
                            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold"/>
                            <!--<TextBlock Text="{Binding Message}" TextWrapping="Wrap" />-->
                            <!--<TextBlock Text="{Binding PostTime}" Foreground="Gray"/>-->
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
